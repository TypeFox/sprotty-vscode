{
  "name": "sprotty-vscode-webview",
  "version": "0.0.5",
  "files": [
    "lib",
    "src",
    "css"
  ],
  "dependencies": {
    "reflect-metadata": "^0.1.12",
    "sprotty": "next"
  },
  "devDependencies": {
    "rimraf": "latest",
    "typescript": "latest"
  },
  "scripts": {
    "prepare": "yarn run clean && yarn run build",
    "clean": "rimraf lib",
    "build": "tsc",
    "watch": "tsc -w"
  }
}
